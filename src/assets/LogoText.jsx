import React from "react";
import Logo from "./images/logo.svg";
const LogoText = () => {
  return <img src={Logo} alt="Logo" className="w-full h-full object-contain bg-no-repeat" />;
};

export default LogoText;
